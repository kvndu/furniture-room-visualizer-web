export default function RoomCanvas2D({ children }) {
  return (
    <div style={{ padding: "14px", border: "2px dashed #cbd5e1", borderRadius: "18px", background: "#f8fafc" }}>
      {children}
    </div>
  );
}
